---
import feactures from '../../config/Features.json'
---

<h1 class="text-center font-bold text-5xl mt-10">Feactures</h1>


<div class="grid md:grid-cols-3">
  {
    feactures.map((feacture) => (
        <div class="bg-slate-800 p-10 rounded-xl my-5 mx-5">
            <h2 class="text-2xl font-bolt my-4">{feacture['\'title']}</h2> 
            <p>{feacture['descri[ption']}</p>   
        </div>
    ))
  }
</div>
